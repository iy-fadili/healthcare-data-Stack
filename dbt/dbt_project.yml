name: 'analytics'
version: '1.0.0'
config-version: 2

profile: 'analytics' # Doit correspondre au nom dans votre profiles.yml

model-paths: ["models"]

models:
  analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table